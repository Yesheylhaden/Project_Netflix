PORT=5000            # Set the port the server will run on (5000 is a common default for local development)
JWT_SECRET=supersecretkey  # Secret key used for signing JSON Web Tokens (make sure to keep this secure and not share in public repositories)
